import React from 'react'

const ChatDisplay = ({ chatDataList }) => {
    return (
        <div>{chatDataList.map((chat, index) =>
            <div key>

            </div>
        )}</div>
    )
}

export default ChatDisplay